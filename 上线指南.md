# PVC 装饰膜官网 · 从零上线指南（小白版）

> 适用对象：完全没做过网站、想自己把站搭起来的人
> 前提：我之前帮你做好的网站是**纯静态网站**（一堆 .html + css 文件，没有数据库、没有后台程序），所以下面教的是"最省事、最省钱"的路线。

---

## 一、先搞懂两个最基本的东西

1. **域名（Domain）** = 你的网址，比如 `gongdaa.com`。
   别人在浏览器输入它就能找到你。要去"域名注册商"买，按年付费，相当于租一个门牌号。

2. **空间 / 主机（Hosting）** = 一台 24 小时开机的电脑，用来存放你网站的所有文件
   （就是我帮你做的一批页面，包含英文版和中文版 `/zh/` 目录）。别人访问你的域名时，这台电脑把文件发给他们看。

> 简单比喻：域名是"店名+地址"，空间是"店面"。两个都要有，店才能开张。
> 好消息：因为你的站是静态的，**空间可以免费**。

---

## 二、域名去哪里买（推荐 3 家）

| 注册商 | 适合谁 | .com 年费 | 优点 | 注意 |
|---|---|---|---|---|
| **Namecheap** | 外贸/英文站首选 | ~$10–13/年（约 ¥75–95） | 英文界面友好、价格透明、送免费隐私保护 | 需 Visa / MasterCard / PayPal |
| **Cloudflare Registrar** | 想最便宜 | 成本价 ~$9–10/年 | 全网最低、自动续费、自带免费加速和 HTTPS | 必须同时用它的 DNS |
| **阿里云 / 腾讯云** | 想要中文客服 | ¥70–90/年 | 中文、国内备案方便 | .com 需实名认证；国内服务器要 ICP 备案 |

**我的建议**：你的站是面向海外 B2B 买家（关键词全是英文），首选 **Namecheap** 或 **Cloudflare Registrar**。
- 买 `.com`（国际认可度最高），例如 `decopvc.com`、`yourbrand-pvc.com`
- 预算够的话，把 `.net`、`.cn` 也买了防别人抢注（可选）

**买域名步骤（以 Namecheap 为例）：**
1. 打开 namecheap.com → 顶部搜索框输入想要的名字 → 点 Search
2. 在结果里选 `.com` → Add to Cart → Checkout
3. 注册账号（用邮箱），填账单信息，付款（信用卡 / PayPal）
4. 付款后域名就是你的了，去 Dashboard 里管理

---

## 三、空间 / 主机去哪里买

因为网站是静态的，你**不需要**买昂贵的独立服务器（ECS/云服务器），下面任选其一：

### 方案 A（最推荐·外贸站）：Cloudflare Pages —— 免费 + 全球加速
- 价格：**免费**额度足够一个小官网
- 优点：全球 CDN 加速（海外打开快）、自动 HTTPS、绑定域名简单、拖拽上传
- 适合：你这种"展示 + 询盘"站，零成本起步

### 方案 B：虚拟主机（最像传统"买空间"）
- 推荐 **Hostinger**（约 $2.99/月，有中文界面和 cPanel 面板，传文件像传 U 盘）
- 适合：想用传统"空间"概念，以后可能加后台 / 动态表单

### 方案 C：阿里云 / 腾讯云 ECS（国内服务器）
- 需要 **ICP 备案**（约 1–2 周审核），流程繁琐
- 仅当你主要服务国内客户且愿意备案时才选

**我的建议**：你是外贸出口站、海外访客为主 → 选 **方案 A（Cloudflare Pages）**，
免费、海外快、不用备案、自动 HTTPS。等以后要做复杂功能再升级。

---

## 四、一步步把网站跑起来（以 方案 A Cloudflare Pages 为例）

**第 1 步：买好域名**（见第二节）

**第 2 步：注册 Cloudflare 账号**
- 打开 cloudflare.com → Sign Up（用邮箱注册）
- 选免费版就够

**第 3 步：把域名交给 Cloudflare 管理（改 DNS）**
- 在 Cloudflare 点 "Add a Site" → 输入你的域名
- Cloudflare 会给你两个 **Nameserver（DNS 服务器）** 地址
- 回到 Namecheap（或你的注册商）→ Domain → DNS → 把 Nameserver 改成 Cloudflare 给的那两个
- 等几分钟到几小时生效（邮箱会收到通知）

**第 4 步：部署网站文件**
- Cloudflare 控制台 → Workers & Pages → Create → Pages → 上传 / 拖拽整个网站文件夹
  （要包含 index.html、css/、各个 .html、zh/ 中文版目录、sitemap.xml、robots.txt）
- 它会先给你一个临时网址（如 `xxx.pages.dev`），点开检查对不对

**第 5 步：绑定正式域名 + 开 HTTPS**
- Pages 项目里 → Custom Domains → 输入 `gongdaa.com` 和 `www.gongdaa.com`
- Cloudflare 自动签发 SSL 证书（地址栏出现 HTTPS 小绿锁），免费

**第 6 步：提交 sitemap 给搜索引擎**
- 登录 Google Search Console → 添加你的域名 → 按提示验证（加一条 DNS 记录或上传验证文件）
- 左侧 "Sitemaps" → 填写 `https://gongdaa.com/sitemap.xml` → 提交
- Bing 同理（Bing Webmaster Tools），提交后海外搜索都能收录

**第 7 步：上线验证**
- 浏览器打开 `https://gongdaa.com`，确认首页、产品页、联系页都正常
- 用手机打开看排版是否自适应

---

## 五、上线前你必须替换的占位内容

我给的代码里有几处占位，上线前请改成你的真实信息（用编辑器"全局替换"即可）：
- `GONGDA` → 你的品牌名
- `gongdaa.com` → 你的真实域名
- 邮箱 / 电话 / WhatsApp / 公司地址 → 真实信息
- 产品图片：上传真实图（建议 WebP 格式，文件名含关键词如 `wood-grain-pvc-film.jpg`，alt 写英文描述）

---

## 六、常见小白疑问

- **Q：域名和空间必须一家买吗？** 不用。域名和空间可以分开买，通过 DNS 解析连起来就行（本指南就是分开买的）。
- **Q：一年要花多少钱？** 域名约 ¥70–95/年；空间用 Cloudflare Pages **免费**。起步成本几乎为 0。
- **Q：需要备案吗？** 只要空间不在中国大陆，就不需要 ICP 备案。本方案选海外/Cloudflare，免备案。
- **Q：询盘表单现在能用吗？** 现在是 `mailto:` 占位（点提交直接调起邮件）。正式上线建议接表单服务（如 Formspree、或 Cloudflare Forms）或你的 CRM。

---

## 七、后续我可以继续帮你做的
- 把网站文件按 Cloudflare Pages 的要求整理好、打包
- 帮你把品牌名 / 真实文案全局替换成"开箱即用"版
- 写博客正文（长尾词池已整理在 SEO-Strategy.md 里）
- 继续扩展更多小语种（西语 / 俄语 / 阿语等，复制 `/zh/` 结构新增 `/es/`、`/ru/`、`/ar/` 即可，无需改样式）
- 补齐剩余中文页（blog / certifications / faq 及部分产品页，目前暂回退到中文首页）
